
{
  "entities": {
    "UserProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "UserProfile",
      "type": "object",
      "description": "يمثل مستخدم النظام ويحدد صلاحياته. الأدوار: Admin (مدير النظام)، Agent (موظف رفع البلاغات في الكول سنتر)، Specialist (أخصائي معالجة وحل البلاغات في الأقسام الفنية).",
      "properties": {
        "id": { "type": "string" },
        "username": { "type": "string", "description": "الهوية المصرفية الموحدة للموظف (BIM ID)" },
        "name": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "role": { "type": "string", "enum": ["Admin", "Agent", "Specialist"] },
        "department": { "type": "string", "description": "القسم التابع له (Support, Cards, Digital, App, Operations)" }
      },
      "required": ["id", "username", "name", "email", "role", "department"]
    },
    "ComplaintTicket": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "ComplaintTicket",
      "type": "object",
      "description": "يمثل بلاغ العميل. يتم إنشاؤه بواسطة الـ Agent (موظف الرفع) ومعالجته بواسطة الـ Specialist (أخصائي المعالجة).",
      "properties": {
        "id": { "type": "string" },
        "ticketID": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" },
        "status": { "type": "string" },
        "customerName": { "type": "string" },
        "cif": { "type": "string" },
        "phoneNumber": { "type": "string" },
        "serviceType": { "type": "string" },
        "subIssue": { "type": "string" },
        "intakeMethod": { "type": "string" },
        "description": { "type": "string" },
        "createdByAgentId": { "type": "string" },
        "createdByAgentName": { "type": "string" },
        "attachments": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "url": { "type": "string" },
              "description": { "type": "string" }
            }
          }
        }
      },
      "required": ["customerName", "cif", "phoneNumber", "serviceType", "intakeMethod", "subIssue", "description"]
    }
  },
  "auth": {
    "providers": ["password"]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": { "entityName": "UserProfile" }
      },
      {
        "path": "/tickets/{ticketId}",
        "definition": { "entityName": "ComplaintTicket" }
      },
      {
        "path": "/settings/system-config",
        "definition": { "entityName": "SystemConfig" }
      }
    ]
  }
}
