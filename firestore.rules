
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // تم فتح القواعد مؤقتاً للسماح للمطور بإكمال بناء الواجهات دون قيود تسجيل الدخول
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
