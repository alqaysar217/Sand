
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // السماح بالوصول الكامل لجميع المستخدمين المسجلين وغير المسجلين مؤقتاً لضمان استقرار التطوير
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
