rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // فتح الصلاحيات بالكامل مؤقتاً لتسهيل عملية الفحص والاختبار لجميع الأقسام
    match /{document=**} {
      allow read, write: if true;
    }
  }
}